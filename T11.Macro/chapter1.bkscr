*main

//--------------------------------------------------------------------------------------------------
通过call进入Chapter1[p]

//--------------------------------------------------------------------------------------------------
测试macro中的跳转指令[lr]
@test_jump_inmacro label=*jump_inmacro_label file="chapter1"
跳转后这里不会被执行[p]

*jump_inmacro_label
接下来，通过return返回调用的地方。[p]
@return