*returnto_label
接下来，跳转到结束[lr]
@jump label=*end
//--------------------------------------------------------------------------------------------------

*main

//--------------------------------------------------------------------------------------------------
所有的macro都必须在macro.bkscr中import或者定义[lr]
@create_textwindow
使用macro来创建textwindow[l][r]
@create_cursor index=1000 filename="LineBreak_a"
使用macro来创建cursor[p]

//--------------------------------------------------------------------------------------------------
接下来，在call之后的代码中使用macro[lr]
@call label=*main file="chapter1"
已经从Chapter1返回。[p]

//--------------------------------------------------------------------------------------------------
如果使用returnto结束macro，可以直接跳转到指定标签。[lr]
@test_returnto label=*returnto_label file="main"
此处代码不会被执行[p]

//--------------------------------------------------------------------------------------------------
*end
演示结束[l]
